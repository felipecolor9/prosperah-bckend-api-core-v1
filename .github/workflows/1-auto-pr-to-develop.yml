name: [LOCAL] Cria PR para develop

on:
  push:
    branches:
      - 'feature/*'

jobs:
  create_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          title: "Merge to develop"
          body: "This PR merges changes from local feature branch to develop"
          base: develop